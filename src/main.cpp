#include <iostream>
#include "StdAfxRegArchLib.h"  // Adjust if needed

int main() {
    std::cout << "Testing RegArch library...\n";
    // If there's a function in RegArch you can call, do it here:
    // e.g. std::cout << "RegArch version: " << RegArchVersion() << std::endl;
    return 0;
}
